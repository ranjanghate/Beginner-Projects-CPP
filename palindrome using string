#include <iostream>
#include <string.h>
using namespace std;
int main()
{
  char a[20] , b[20] ;
  cin>>a;
  
  for(int i=strlen(a) -1 , j=0 ; i >=0 , j<strlen(a)-1 ; i-- , j++)
    b[j]=a[i];
    
   if(strcmp(a,b))
		cout<<"It is Palindrome";
	
	else 
	cout<<"Not a palindrome";	  
   
}
